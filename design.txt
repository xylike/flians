1.档案设置
	1>档案编码设置
		a.客户档案
		b.人员档案
		c.存货档案
		d.班组档案
		e.供应商档案
		f.人员工号
		g.职位

详细信息:
	1.完全手工编号
	2.自动生成编号,允许手工改动
	3.单前缀不固定长度编码

2.单据编号设置 
	

详细信息:
	1.完全手工编号
	2.手工改动,重号时自动重取



六.菜单调整工具:

菜单描述,如:
系统菜单
非末级节点

1.是否末级
2.程序类型,如是末级,才可以选程序类型:
3.菜单编号
4.菜单名称 
5.菜单英文名称
6.菜单繁体名称
7.参数
8.DLL类名或exe路径

一.表单设计器
二.栏目设计器
三.参照设计器
四.过滤设计品
五.敏感数据维护工具



----------------------------------
表单设计
	1.实体
		主表
			栏目
				栏目编号
				栏目名称
				栏目类型
				字段名称
				栏目描述 
		子表
			栏目
				列表 (新增 复制 修改 删除 上移 下移 全选 全消 确定 应用 取消 导出SQL)
					启用
					名称
					栏目名称
					列宽
					对齐方式
					排序
					合计
					字段名
				编辑
					是否启用
					名称
					标题
					对应字段
					列宽
					对齐方式
						左对齐
						右对齐
						中间对齐
					排序
						升序
						降序
					是否枚举
					是否合计
					是否必选
					可编辑?
					是否隐藏
					枚举类型 ________
					枚举字符串
					参照类型
					汇总
						分组(group by)
						支持
						不支持
						合计
						平均
						最大
					小数位
					格式化
					子产品号
					项目号
				上一条  下一条 确定  取消 

	2.功能
		表单功能
			功能管理(增加 删除 修改)
				打印分组
					打印
					预览
				输出
				分割条1
				增加
				复制
				修改
				删除
				放弃
				保存
				分割条2
				提交
				重新提交
				撤消
				审核
				弃审
				查审
				分割条3
				联查
				分割条4
				生单
				分割条5
				定位
				刷新
				分割条6
				视图
				格式
				分割条7
				首张
				末张
				上张
				下张
				分割条8
				选项
			状态设置(功能有可用 不可用 隐藏三种状态) 
			名称   新增状态   修改状态  审核状态   浏览状态   空白状态
		列表功能
			功能管理
	3.栏目设置(需要发布以后才可以设置)

	4.过滤(查询条件设置器)
		
	5.编号规则





枚举表(UA_Enum): 
	enumType varchar(32) primarykey,
	enumCode varchar(32),
	localeId varchar(32) primarykey,
	enumName varchar(60),
	enumIndex int primarykey



菜单表(UA_Menu):
	autoId int  primarykey,
	menuId varchar(12) not null,
	menuName varchar(60) not null,
	supMenuId varchar(12), //#1 为分类，否则为分类编号
	sortindex int,
	imgIndex int,
	paramters varchar(1024)


 
实体表(EntityTable)
	


